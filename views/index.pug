<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="UTF-8")
    title LandingPage
    link(
       rel="stylesheet"
       href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-alpha.6/css/bootstrap.min.css"
       integrity="sha384-rwoIResjU2yc3z8GV/NPeZWAv56rSmLldC3R/AZzGRnGxQQKnKkoFVhFQhNUwEyJ"
       crossorigin="anonymous")

    //- move styles to asset repository
    style.
      .container { min-width: 1140px; }

      product-list .products { list-style-type: none; padding: 0; }
      product-list product-item { width: 28%; margin-left: 1%; margin-bottom: 1rem; float: left; height: 250px; }
      product-list-fetch-indicator:before { content: ''; display: block; clear: both; }
      .card { border-radius: 0; background-color: rgba(134, 134, 134, 0.1); color: #fff; }
      .card-footer { background-color: #fff; }

      bookmark-form .card-footer:last-child {
        border-radius: 0;
      }

      product-item {
        overflow: hidden;
        position: relative;
        margin-bottom: 1rem;

      }

      product-item .photo {
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        z-index: -1;
        max-width: 100%;
      }

      product-item bookmark-form {
        position: absolute;
        bottom: 0;
        left: 0;
        right: 0;
      }

      product-item .price {
        position: absolute;
        right: 1rem;
        bottom: 3rem;
      }

  body
    .container
      include partials/nav

      <!--# include virtual="/order/basket" -->

      h2 scs commerce
      p Lorem ipsum dolor sit amet, consectetur adipisicing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat. Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur. Excepteur sint occaecat cupidatat non proident, sunt in culpa qui officia deserunt mollit anim id est laborum.

      h2  Products

      <!--# include virtual="/products/" -->

    script(src="systemjs/dist/system.js")

    script.
      //- var dependencies = []
      System.config({
        map: {
          'zepto': 'zepto/dist/zepto.js',
          'document-register-element': 'document-register-element/build/document-register-element.js',
          'fetch': 'whatwg-fetch/fetch.js'
        },
        bundles: { 'javascripts/custom-elements.js':
           [ 'javascripts/custom-elements/product-list/endless-list-fetcher.js',
             'javascripts/custom-elements/product-list/index.js',
             'javascripts/custom-elements/product.js' ] }
      })

      const dependencies = [SystemJS.import('document-register-element')]

      if (typeof fetch !== 'function') dependencies.push(SystemJS.import('fetch'))

      Promise.all(dependencies)
        .then(() => SystemJS.import('./javascripts/main.js'))
